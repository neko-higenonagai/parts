# AGENTS.md

## 基本方針

1. **Git コミットの手動運用**:
   - Git のステージング（`git add`）、コミット（`git commit`）、タグ付け（`git tag`）、プッシュ（`git push`）は、AIアシスタント側で自動実行せず、ユーザーが手動で行います。
   - 作業完了時もコミットは行わず、変更箇所の提示と検証結果の報告にとどめます。

2. **バージョン変更の手動運用**:
   - バージョン番号（`VERSION` ファイル、`project.godot` の `config/version`、UI のバージョン表記等）の更新・インクリメントは、AIアシスタント側で自動的に変更せず、ユーザーが手動で行います（ユーザーから明示的な更新指示があった場合を除く）。

3. **AI アシスタントの対応範囲**:
   - ソースコードの実装・修正、機能追加、バグ修正、および動作検証（テスト実行やエラー確認）に専念します。
